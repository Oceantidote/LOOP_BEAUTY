suggestionsContainer = document.getElementById('suggestions')
suggestionsContainer.innerHTML = ''
if (<%= @query.length > 2 %>) {
  suggestionsContainer.innerHTML = `<%= j render 'shared/each_suggestion', products: @search_products %>`
}
